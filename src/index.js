// const jwt = require('jwt');
const crypto = require('crypto');
// var base64url = require('')
console.log('at',crypto.randomBytes(64).toString('hex'));
console.log('rt',crypto.randomBytes(64).toString('hex'));

